{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix ../src --no-fund --no-audit",
    "prestart": "npm install --prefix ../src --no-fund --no-audit",
    "start": "npm run dev --prefix ../src",
    "dev": "npm run dev --prefix ../src",
    "build": "npm run build --prefix ../src",
    "preview": "npm run preview --prefix ../src",
    "lint": "npm run lint --prefix ../src",
    "format": "npm run format --prefix ../src"
  }
}
